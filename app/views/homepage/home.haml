.topPadding
  #content-box
    - if session[:user]
      - form_tag :action => 'pivotal' do
        = flash[:message]
        = flash[:warning]

        %br
        %center
          %table
            %thead
              %tr
                %th{:colspan => '2'}
                  Welcome,
                  =session[:screen_name] + '.'
                  Connect with Pivotal Tracker right now!
            %tbody
              %tr
                %td Input your API Token:
                %td= text_field :sessions, :token
                %td{:align => 'left'}= submit_tag 'Submit', :class => 'ui-button'
            %tfoot
              %tr
                %td{:colspan => '2', :align => 'center'}
                  %h7 It is quite imperative that you input your own token before proceeding. :)
      %br
        
    - else
      %h3 Home Page
